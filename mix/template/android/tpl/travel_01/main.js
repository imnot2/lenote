lenoteTemplate.api.init = function(deviceWidth, deviceHeight){    //初始化相册的高与宽    //var gallery = document.querySelector('.gallery'),    //    deviceWidth = this.device.getWidth(),    //    deviceHeight = this.device.getHeight();    //gallery.style.width = (deviceWidth - 100) + 'px';    var gallery = document.querySelector('.gallery'),        gallery_width = this.helper.getDomWidth(gallery) - 40,        gap = 4, column = 3, border_width = 2,        images = gallery.querySelectorAll('[_type=image]'),        //per_width = parseInt((gallery_width - gap * 2 - border_width * 6) / column);        per_width = (gallery_width - gap * 2 - border_width * 6) / column;        for(var i = 0; i < images.length; i++){        var image = images[i], id = image.getAttribute('id');        if(id == 'image_1'){            image.style.width = image.style.height = (per_width * 2 + gap + border_width * 2) + 'px';        }else{            image.style.width = image.style.height = per_width + 'px';        }        image = null;    }    gallery = images = null;        //var main = document.querySelector('.main'), offset = per_width / 3, main_height = this.helper.getDomHeight(main);    //main.style.top = -offset + 'px';    //main.style.paddingTop = (offset + 28) + 'px'; //加上28px的间距    //main = null;        var white_bg = document.querySelector('.gallery_bg .white_bg');    white_bg.style.height = (per_width / 3 + 20) + 'px';    white_bg = null;        var fromLWidth = this.helper.getDomWidth(document.querySelector('.dest label.from')),        toLWidth = this.helper.getDomWidth(document.querySelector('.dest label.to')),        padding = 20, destTextwidth = (gallery_width + 20 - fromLWidth - toLWidth - padding) / 2 //padding包括边框和padding        from = document.querySelector('.dest #from'),        to = document.querySelector('.dest #to');    from.style.width = to.style.width = destTextwidth + 'px';    from = to = null;        var contacts = document.querySelector('#contacts'), label = document.querySelector('.members label');    contacts.style.width = (gallery_width + 20 - this.helper.getDomWidth(label) - 10) + 'px'; //减去padding    contacts = null;        //var main = document.querySelector('.main'),    //    content = document.querySelector('#content');    //content.style.minHeight = (this.helper.getDomHeight(main) - content.offsetTop) + 'px';    //main = content = null;};