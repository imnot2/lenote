if action === 'index'    #footer        if !F.isEmpty(client)            a.client(href="#{client.link}" title="#{client.desc}")                img(src="images/client.png" alt="#{client.desc}")                span.close(title="关闭")else if action === 'list'    if F.isEmpty(client)        #footerClean    else        #footerLite            span.download 需要更多笔记功能？立即使用                a.client(href="#{client.link}" title="#{client.desc}") 乐云记事客户端 &raquo;else if action === 'show'    #footerLite.detail        #notetime            span.icon 更新于            span.timeelse    #footerCleaninput#csrf(type="hidden" value="#{csrf}")input#ip(type="hidden" value="#{ip}")script(src='/m/js/require.js' data-main='/m/js/main')//-Template for tipsscript#notifyscript(type="text/html").    <div id="overlay" class="<%= notify.overlayClass %>"></div>    <div id="notify">        <div class="circle"></div>        <div class="content">            <span class="title"><%= notify.title %></span>            <% if(notify.tips){ %>                <span class="tip"><%= notify.tips %></span>            <% } %>        </div>    </div>//-Template for downAppif !F.isEmpty(client)    script#downbox(type="text/html" link="#{client.link}").        <div class="downWrap">            <a class="closeBtn" href="javascript:;"></a>            <p class="info">仅支持待办的查看和阅读<br/>如需编辑和创建<br/>请下载乐云记事官方APP</p>            <a class="downBtn" href="#{client.link}">前往下载</a>        </div>