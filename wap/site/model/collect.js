//https://github.com/indooorsman/node-readability-cheerio//https://github.com/luin/node-readability//https://github.com/leizongmin/js-xss/blob/master/README.zh.mdvar fetch = require('node-readability-cheerio'),    readability = require('node-readability'),    copy = require('copy-to'),    thunk = require('thunkify'),    default_options = {        timeout: 1000 * 30, //30秒        outputEncoding: 'utf-8',        headers:{            'User-Agent': 'Mozilla/5.0 (Windows NT 6.1) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/35.0.1916.153 Safari/537.36'        }    };fetch.read = thunk(fetch.read);readability = thunk(readability);module.exports = function *(html, options){    options = options || {};    copy(default_options).to(options);        var j = yield fetch.read(html, options)        result = yield readability(j('body').html());        return result[0];};