var fs = require('fs');
var mysql = require('./index.js');
var conn = mysql.createConnection({
   user: 'root',
   password: '',
   database: 'test',
   host: '127.0.0.1',
   port: '3307',
   debug: true,
   compress: 1,
 });

setTimeout(function() {
conn.query('select * from mil limit 1000', function(err, res) {
  console.log(res);
});
}, 10);
